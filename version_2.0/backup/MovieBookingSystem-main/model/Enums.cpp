#include <iostream>

using namespace std;

enum Language = {HINDI, ENGLISH, BENGALI, MARATHI, TELEGU, TAMIL, GUJRATI};
enum Genre = {ACTION, COMEDY, FICTION, BIOGRAPHY, WAR};
enum SeatType = {VIP, DELUXE, BALCONY};
enum SeatStatus = {AVAILAIBLE, OCCUPIED};
enum PaymentMode = (CREDIT, DEBIT, CASH, NETBANKING, UPI);
enum PaymentStatus = (SUCCESS, FAILED, PENDING);
